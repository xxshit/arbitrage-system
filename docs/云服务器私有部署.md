# 云服务器私有部署记录

更新时间：2026-08-02（Asia/Shanghai）

## 服务器概况

- 地址：`5.61.208.92`
- SSH端口：`16206`
- 系统：Ubuntu 24.04.3 LTS
- 运行环境：容器；可见32线程AMD Ryzen 9 9950X，但CPU未设置cgroup配额，不代表独占整机。
- 内存硬限制：4GB；部署前已有14次历史OOM记录。
- 磁盘：约1.8TB，审计时使用约398GB。
- 既有业务：约8组应用套件，以及Nginx、MariaDB、Redis、PM2等基础服务。

## 套利系统部署

- 项目目录：`/opt/arbitrage-hub`
- Linux运行账户：`arbhub`
- PM2服务名：`arbitrage-private`
- 应用服务器：Gunicorn，1个worker、4线程。
- 内存保护：保持单worker以避免后台采集与推送重复执行；不再按请求次数回收worker，由PM2的420MB内存上限负责异常内存保护。
- 私有监听：`127.0.0.1:15831`
- 数据库：复用服务器MariaDB，独立数据库`arbitrage_hub`、独立本机用户`arbi_hub`。
- 数据迁移结果：27张表、1个网站账户、1218条现多期空快照、1324条期多期空快照、83175条历史资费记录。
- 配置权限：`.env`为`600`；项目目录为`750`；不在Nginx中公开，不新增公网端口。

## 数据库备份状态

- PM2服务名：`arbitrage-db-backup`
- 云端备份目录：`/var/backups/arbitrage-hub/mysql/`，仅root可读。
- 每6小时热备一次，短周期备份保留14天；每日归档保留90天。
- 本机计划任务：`ArbitrageHub-CloudMySQLBackup`，每日09:30拉回，错过后开机补跑。
- 本机备份目录：`F:\套利系统\backups\cloud-mysql\`，保留180天且不进入Git。
- 2026-08-01首份备份已在云端和本机分别通过gzip及SHA-256完整性校验。

详细恢复流程见`docs/数据库备份与灾难恢复.md`。

## 访问方式

日常使用优先打开公网HTTPS地址：

`https://arbi-k7m4p2.5-61-208-92.sslip.io:18443/`

公网入口只反向代理网站，不开放Gunicorn或MySQL。下面的SSH隧道保留为维护和公网入口故障时的备用通道。

本机双击项目根目录的`打开云端网站.vbs`，会隐藏建立SSH隧道并打开：

`http://127.0.0.1:5000`

也可以手动执行：

```powershell
ssh -N -i "$env:USERPROFILE\.ssh\arbitrage_deploy_5_61_208_92" -p 16206 `
  -o BatchMode=yes -o ServerAliveInterval=60 -o ServerAliveCountMax=3 `
  -L 127.0.0.1:5000:127.0.0.1:15831 root@5.61.208.92
```

网站、数据库和后台采集始终在云端运行。本机SSH隧道只负责访问，关闭电脑不会停止云端任务。

## 运维命令

```bash
pm2 status arbitrage-private
pm2 status arbitrage-db-backup
pm2 restart arbitrage-private
pm2 logs arbitrage-private --lines 100
pm2 logs arbitrage-db-backup --lines 100
curl -I http://127.0.0.1:15831/login
```

数据库和环境密码不得写入Git、文档或聊天记录。部署密钥私钥仅保存在Windows个人SSH目录，不在项目仓库中。

## 资源安全结论

预热后容器内存约3.48GB/4GB，新应用Gunicorn主进程与worker合计约210MB，OOM计数没有增加。余量仍然偏薄，后续增加大型任务前必须重新检查`memory.current`和`memory.events`；如果发生新OOM，应优先停止`arbitrage-private`并分析，而不是影响原有项目。

## 2026-08-02 盯盘逻辑部署闭环

- 部署前已执行一次云端MySQL热备份，备份脚本正常完成。
- 云端曾仍运行旧版AKE绝对价位模板，说明GitHub已更新不代表线上已经更新；本次已同步新`app.py`并由PM2重启`arbitrage-private`。
- 部署后确认本地文件与云端文件SHA-256一致，私有监听与公网HTTPS均返回`200`。
- Gunicorn显示一个主进程和一个工作进程，属于`1 worker + 4 threads`的正常父子关系，不是重复启动两套后台采集器。

## 2026-08-03 聊天短暂断线修正

- 事实：协作记录启用1秒增量读取后，Gunicorn的`--max-requests 500`会很快触发worker回收；服务只有一个worker，回收交接期间没有第二个worker承接请求，浏览器可能出现`Failed to fetch`。
- 修正：保持单worker、4线程，移除按500次请求自动回收的参数，避免复制后台采集和Lark推送任务；继续保留PM2的420MB内存重启保护。
- 前端保护：联系人和消息读取遇到短暂断线时自动重试；发送连接中断时不再弹出原始英文错误，而是保留输入内容、自动核对发送结果，并用页面状态提示用户确认。

## 2026-08-10 iPhone协作与趋势扫描部署

- GitHub `master` 与云端同步发布 iPhone 协作声音/竖屏修正、30分钟扫描线程自动恢复，以及五根30MIN强结构价格门槛 `50% → 40%`；其它资金结构硬条件不变。
- 116项测试通过；部署前备份与原子部署内置备份均成功，最新备份通过gzip完整性校验。
- 部署后线上运行文件与本地 SHA-256 一致，公网HTTPS登录页返回200；Gunicorn与MariaDB继续只监听 `127.0.0.1`。
- 云端 `intraday_early_trend_scan` 在重启后成功完成新一轮扫描，行情快照继续推进；SOON的停用状态和停止价未被部署改动。

## 2026-08-10 AKE独立趋势学习部署

- AKE短线3小时、中线24小时、长线3天改为各自独立续期；旧长线未结束时，新的短线和中线可以按最新结构重新立案，不覆盖历史记录。
- 118项测试通过，部署前和原子部署时均完成MySQL热备；线上应用指纹与本地一致，HTTPS返回200，行情快照继续更新。
- 首轮任务成功补建短线与中线计划，原长线 `id=12` 保持活动；完成样本及 `57.1%` 准确率基线未被部署重写。
