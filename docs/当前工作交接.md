# 当前工作交接

更新日期：2026-08-07（UTC+8）

## 当前基线

- 正式分支：`master`。
- 云端应用运行基线为 `b879490`，GitHub `master` 与云端 6 个运行文件已完成 SHA-256 一致性核对；该版本包含市值来源校验修正、iPhone 页面适配及登录页纳入原子部署脚本。
- 云端网站使用与本地正式提交一致的应用代码；本机通过被 Git 忽略的 `.local_web_disabled` 标记停用本地网站，不再修改正式代码制造版本差异。
- 云端 MySQL 是实时运行数据的唯一事实来源；本地 MySQL 作为开发与灾难恢复副本，从云端一致性备份同步。
- 云端 MySQL、聊天密钥和本地回退库均有独立备份，不进入 Git。
- 本地 MySQL 已恢复并验证到 2026-08-05 09:45 云端一致性备份；云端随后仍持续写入，因此换电脑或继续本地开发前需要重新运行同步脚本取得新的时间点快照。

## 最近完成

- AKE 短线、中线、长线趋势验证已形成自动批次、路径事件、止盈止损结果和校准闭环。
- 协作记录已支持加密、七天保留、图片与表情、引用、个人删除、双方撤回、置顶、已读图标和独立声音开关。
- 现多期空与期多期空的排序、资费差、币种详情、下架风险标记和行情局部刷新已完成最近一轮调整。
- 币种详情已显示各交易所结算周期；资费详情支持分别选择做多、做空交易所；机会看板左侧显示涨势结构评分 Top 10，右侧显示三所合约 OI / CoinGecko 流通市值 Top 10。涨势结构榜按 `4H` 数值方向着色：持仓增加、人数比上升、CVD 主动买入为绿字，反向为红字；颜色不改变评分公式。
- `PLAY/PLAYSOUT` 与 `SKYAI/SKYAI1` 已作为同资产别名写入系统，运行规则页支持手工新增与管理同币异名记录。
- HEI 风险快速盯盘已开启；TLM 已按用户要求在云端运行状态中停止盯盘，后续等待走出区间再恢复。
- 新增双电脑切换流程：GitHub 同步代码和文档，云端 MySQL 同步运行数据，交接文档恢复不同 Codex 账号的上下文。
- 新增 `scripts/start_workstation.ps1` 与 `scripts/sync_cloud_to_local.ps1`，真实验证通过“云端备份下载与校验 → 本地旧库回退备份 → 云库恢复 → 聊天密钥匹配”完整链路；云端备份脚本权限也已固化为可执行模式。
- 2026-08-07 同步验证结果：GitHub `master`、本地与云端运行文件一致，104 项标准测试通过；部署前云端 MySQL 备份成功，行情写入继续推进，Gunicorn 与数据库保持本机监听，公网 HTTPS 登录页返回 200，应用错误日志为空。
- 2026-08-07 iPhone 版本部署结果：112 项标准测试通过；部署前及原子替换前均完成云端 MySQL 热备，本机副本与独立聊天恢复密钥通过校验。公网 HTTPS 登录页和新版样式返回 200，iPhone 安全区与缓存版本标记已生效；Gunicorn、MariaDB 保持 `127.0.0.1` 监听，行情快照在重启后继续推进，应用错误日志为空。

## 已知边界

- 行情和趋势判断必须区分用户假设、Codex 假设、验证结果与被推翻结论；未经验证不能升级为固定知识。
- 趋势推送必须出现新的结构、数据变化或观点变化；同结构同判断不得机械重复推送。
- 本地网站当前按用户要求保持停用；这不影响本地 MySQL 备份与同步。
- iPhone 当前为响应式网页适配；添加到主屏幕可以正常使用前台聊天，但尚未接入 Service Worker、Web Push 或 APNs，网页被 iOS 挂起时不能承诺后台实时收件。
- 任何恢复都必须先备份目标数据库；聊天 SQL 与聊天密钥必须成对保存。

## 新会话接手顺序

1. 阅读 `AGENTS.md` 与 `docs/README.md`。
2. 阅读本文件及当前任务相关文档。
3. 以 MySQL 最近执行时间和实时状态为准，不以旧对话截图替代运行事实。
4. 修改前检查 `git status`；不得覆盖用户未提交内容。
5. 完成后测试、更新文档、提交 Git；需要换电脑时先推送并更新本交接文件。

## 本次会话压缩摘要

- 双电脑的职责已经固定：GitHub 保存代码与长期记忆，云端 MySQL 保存实时运行事实，本地 MySQL 仅作为可回退开发副本。
- 已完成的电脑以后可以直接运行 `scripts/start_workstation.ps1`；全新电脑必须先安装 Git、Python、MySQL/MariaDB，创建本地数据库，并单独配置 `.env`、云端连接配置和 SSH 私钥。
- 同一时间只让一台电脑直接维护 `master`。脚本发现工作区修改、本地未推送提交或分支分叉时会停止，不做强制覆盖。
- 数据库同步只允许“云端到本地”，不会把旧本地库自动写回云端；所有覆盖动作前都保留本地回退库。
- 密钥、密码、Webhook、数据库文件和备份均没有进入 Git。不同 Codex 账号通过本文件、`AGENTS.md` 和知识库索引恢复上下文，不依赖原始聊天记录。

## 下一步重点

- 持续积累 AKE 多周期验证的有效样本，并根据真实路径校准目标、软警戒和硬止损，不因单次样本频繁翻转中线/长线方向。
- 继续检查自动任务最近执行时间、数据库备份链路和推送去重状态。
- 若引入第二台开发电脑，先配置各自私密文件，再运行 `scripts/start_workstation.ps1`；不得复制或提交密钥到 Git。
